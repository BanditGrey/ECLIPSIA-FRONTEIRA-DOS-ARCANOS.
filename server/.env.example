PORT=5000
# Obrigatória em produção (Atlas). Se ausente, o servidor usa um MongoDB
# efêmero em memória (mongodb-memory-server) — só para testes.
MONGO_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/eclipsia
JWT_SECRET=MUDE_ISSO
CLIENT_URL=http://localhost:5173
ADMIN_KEY=troque_por_uma_chave_secreta
